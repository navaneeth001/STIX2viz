## What does this PR change?

<!-- One or two sentences. -->

## Checklist

- [ ] `npm run verify` passes locally (lint, format, typecheck, tests, build, bundle budget, publint, attw)
- [ ] All existing props, callbacks and rendered DOM remain backward compatible (new features behind new, default-off props)
- [ ] No new runtime dependencies
- [ ] Tests added/updated — behaviour changes to `makeGraphData` update `stix2viz.test.ts` deliberately
- [ ] README/CHANGELOG updated for user-visible changes
